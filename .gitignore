# Env / secrets
.env
*.env
!.env.example
*.pem
*.key

# Go
/bin/
*.test
*.out
coverage.*
vendor/

# Node / web
node_modules/
*.tsbuildinfo
.pnpm-store/
# NOTE: server/internal/webui/dist IS committed — it is embedded into the Go
# binary via go:embed, so `go build`/`go test` work without a node toolchain.

# Build artifacts
/opentdm
/opentdm-server

# Editors / OS / tooling
.DS_Store
.idea/
.vscode/
*.swp
# Commit shared project config (.claude/{skills,agents,hooks,settings.json});
# ignore only per-developer local settings.
.claude/settings.local.json

# GoReleaser output
/dist/

# Playwright output
.playwright-mcp/
*.png
